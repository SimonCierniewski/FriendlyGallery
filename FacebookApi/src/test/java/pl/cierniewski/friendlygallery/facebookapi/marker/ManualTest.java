package pl.cierniewski.friendlygallery.facebookapi.marker;

/**
 * This interface presents tests that have to be invoked manually, because they are create too much
 * work on staging server.
 */
public interface ManualTest {
}
